const DetectArea = ({ detectAreaRef }) => {
  return (
    <div
      ref={detectAreaRef}
      className="w-full text-white py-4 flex justify-center items-center text-xs"
    >
      DetectArea
    </div>
  );
};

export default DetectArea;
